# Credentials
.env
.env.*
!.env.example
.secret/
*.pem
*accessKeys*

# OS
.DS_Store
Thumbs.db

# Project outputs (사용자별 산출물)
docs/plans/
docs/designs/
docs/verifications/

# 세션 상태 파일 (사용자별, 커밋 금지)
NOVA-STATE.md
.nova-orchestration.json

# record-event.sh CI 분기 출력 (CI=true + CI_ARTIFACTS 미지정 시 프로젝트 루트에 생성됨)
nova-events/

# 주의: mcp-server/dist/ 는 플러그인 entrypoint (.mcp.json 참조)이므로 커밋 대상이다.
# 과거에 이 경로가 ignore 되어 있어 v5.15.1/v5.15.2 릴리스에 빌드 산출물이 반영되지 않는
# 사고가 발생했다. 되돌리지 말 것.

# bkit generated files
docs/.bkit-memory.json
docs/.pdca-status.json

# Claude Code local settings (per-user)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# IDE
.vscode/
.idea/

# Dependencies
node_modules/

# MCP server
mcp-server/node_modules/
# mcp-server/dist/ 는 ignore하지 않는다 — 플러그인 entrypoint이므로 반드시 커밋한다 (위 주석 참조)

# Nova Orbit (별도 프로젝트)
nova-orbit/
.nova/

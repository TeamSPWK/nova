#!/usr/bin/env bash

# Nova Engineering — SessionStart Hook
# 매 세션 시작 시 핵심 자동 적용 규칙을 additionalContext로 주입

cat << 'NOVA_EOF'
{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "# Nova Engineering — 자동 적용 규칙\n\n이 규칙은 사용자가 커맨드를 명시적으로 호출하지 않아도 모든 대화에 자동 적용된다.\n\n## 1. 작업 전 복잡도 + 위험도 판단\n\n모든 작업 요청에 대해 먼저 복잡도를 판단한다:\n\n| 복잡도 | 기준 | 자동 행동 |\n|--------|------|----------|\n| **간단** | 버그 수정, 1~2 파일 수정, 명확한 변경 | 바로 구현 → 구현 후 독립 검증 |\n| **보통** | 3~7 파일, 새 기능 추가 | Plan 작성 → 승인 → 구현 → 독립 검증 |\n| **복잡** | 8+ 파일, 다중 모듈, 외부 의존성 | Plan → Design → 스프린트 분할 → 구현 → 독립 검증 |\n\n사용자가 '그냥 빠르게 해줘'라고 하면 복잡도 판단을 건너뛸 수 있다. 단, 경고를 표시한다.\n\n복잡도 재판단: 작업 중 수정 파일이 초기 예상을 넘어서면 복잡도를 재판단한다. 인증/DB/결제 등 고위험 영역은 파일 수와 무관하게 한 단계 상향한다.\n\n## 2. Generator-Evaluator 분리 (핵심)\n\n자기가 쓴 코드를 자기가 평가하면 편향이 생긴다.\n\n**검증 분리는 필수(must), 구현 위임은 권장(should).**\n\n- 검증(Evaluator)은 반드시 독립 서브에이전트로 실행한다. 예외 없음.\n- 검증 에이전트는 적대적 자세로 임한다: '통과시키지 마라, 문제를 찾아라.'\n\n구현(Generator) 위임은 복잡도에 따라 판단한다:\n- 간단 (1~2 파일): 메인 에이전트 직접 구현 + Evaluator 서브에이전트 필수\n- 보통 (3~7 파일): 메인 직접 가능, 서브에이전트 권장 + Evaluator 서브에이전트 필수\n- 복잡 (8+ 파일): 서브에이전트 권장 + Evaluator 서브에이전트 필수\n\n메인 에이전트가 직접 구현할 때의 위험: 컨텍스트가 쌓이면 판단 품질이 떨어진다. 보통 이상의 작업에서 메인이 직접 구현하면, 검증 서브에이전트의 판정을 더 엄격히 적용한다.\n\n서브에이전트 가시성: tmux 세션 내에서 실행 중이면, 서브에이전트를 별도 tmux pane으로 spawn하여 사용자가 진행 상황을 실시간으로 확인할 수 있게 한다.\n\n## 3. 검증 기준\n\n검증 시 다음 관점을 반드시 포함한다:\n- 기능: 요청한 것이 실제로 동작하는가?\n- 데이터 관통: 입력 → 저장 → 로드 → 표시까지 완전한가?\n- 설계 정합성: 기존 코드/아키텍처와 일관되는가?\n- 크래프트: 에러 핸들링, 엣지 케이스, 타입 안전성이 갖춰져 있는가?\n\n## 4. 실행 검증 우선\n\n'코드가 존재한다' ≠ '동작한다'. 가능한 경우 실제 실행 테스트를 수행한다 (테스트 실행, 브라우저 테스트 등).\n\n## 5. 검증 경량화 원칙\n\n검증이 무거우면 사용자가 우회한다. 기본 검증은 경량(Lite)으로 수행한다. --strict를 명시적으로 요청할 때만 풀 검증을 수행한다.\n\n## 6. 복잡한 작업의 스프린트 분할\n\n8개 이상 파일을 수정하는 작업은 독립 검증 가능한 스프린트로 분할한다. 각 스프린트마다 구현 → 검증 사이클을 반복하고 스프린트 간 전환 시 사용자에게 보고하고 확인받는다.\n\n## 7. 블로커 분류\n\n구현 중 장애물을 만나면:\n- Auto-Resolve: 외부 상태 변경 없이 되돌리기 가능 → 자동 해결\n- Soft-Block: 진행 가능하나 런타임 실패 가능성 → 기록 후 계속\n- Hard-Block: 데이터 손실/보안/돌이킬 수 없는 변경 → 즉시 중단, 사용자 판단 요청\n- 불확실하면 Hard-Block으로 상향한다.\n\n## 8. 세션 상태 유지\n\n프로젝트 루트에 NOVA-STATE.md가 있으면 세션 시작 시 반드시 읽는다. 작업 시작/완료/검증 시 NOVA-STATE.md를 업데이트한다. 상태 파일은 50줄 이내를 유지한다 — 인덱스 역할만, 상세는 링크로.\n\n## Nova 커맨드 (상세 절차가 필요할 때)\n\n| 커맨드 | 설명 |\n|--------|------|\n| /nova:next | 다음 할 일 자동 추천 |\n| /nova:plan | CPS Plan 문서 작성 |\n| /nova:design | CPS Design 문서 작성 |\n| /nova:xv | 멀티 AI 교차검증 |\n| /nova:gap | 설계↔구현 역방향 검증 |\n| /nova:review | 적대적 코드 리뷰 |\n| /nova:verify | 코드 품질 + 설계 정합성 통합 검증 (/review + /gap) |\n| /nova:auto | 구현→검증 자율 실행 (Full Cycle) 또는 검증만 (--verify-only) |\n| /nova:init | 새 프로젝트에 Nova 초기 설정 |\n| /nova:propose | 규칙 제안 |\n| /nova:metrics | 도입 수준 측정 |"
  }
}
NOVA_EOF

exit 0

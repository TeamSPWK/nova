# Fixture: visual-intent-new-screen

Brand-new screen creation — typical greenfield UI work.

## User prompt

```
사용자 프로필 페이지 새로 만들어줘. shadcn 스타일로.
```

## Expected G1 capture

- vocabulary auto-extraction → "shadcn" matched directly from prompt
- scope → new files (no existing detect)
- design_system → detect-design-system.sh result
- reference → empty (자연어만)

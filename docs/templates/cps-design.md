# [Design] {기능명}

> AXIS Engineering — CPS Framework
> 작성일: {YYYY-MM-DD}
> Plan: {plans/slug.md}
> Verification: {verifications/slug.md — Gap 검증 후 경로 추가}

---

## Context (설계 배경)

### Plan 요약
- 핵심 문제: {Plan에서 정의한 핵심 문제}
- 선택한 방안: {Plan에서 채택한 솔루션}

### 설계 원칙
- {이 설계에서 우선시하는 원칙 1}
- {이 설계에서 우선시하는 원칙 2}

---

## Problem (설계 과제)

### 기술적 과제

| # | 과제 | 복잡도 | 의존성 |
|---|------|--------|--------|
| 1 | {과제} | 높음/중간/낮음 | {없음 or 과제#} |
| 2 | {과제} | 높음/중간/낮음 | {없음 or 과제#} |

### 기존 시스템과의 접점
- {영향받는 컴포넌트/모듈}
- {호환성 고려사항}

---

## Solution (설계 상세)

### 아키텍처

```
{ASCII 다이어그램 또는 구조 설명}
```

### 데이터 모델

```
{스키마, 인터페이스, 타입 정의}
```

### 데이터 계약 (Data Contract)

> 구현자가 잘못된 가정을 하지 않도록, 주요 필드의 단위·포맷·변환 규칙을 명시한다.

| 필드 | 타입 | 단위/포맷 | 변환 규칙 | 비고 |
|------|------|-----------|-----------|------|
| {필드명} | {타입} | {단위 또는 포맷} | {변환 공식 또는 "없음"} | {주의사항} |

<!-- 예시 — 프로젝트 도메인에 맞게 교체하세요
| price      | number | KRW(원)       | UI 표시 시 ÷10000 → 만원 | 정수, 소수점 절사 |
| latency    | number | ms(밀리초)     | 없음                      | 0 이상           |
| created_at | string | ISO 8601 (UTC) | 표시 시 KST 변환          | timezone 주의    |
-->

### API 설계

| Method | Endpoint | 설명 |
|--------|----------|------|
| {GET/POST} | {/api/...} | {설명} |

### 핵심 로직

```
{의사코드 또는 플로우}
```

### 에러 처리
- {예상 에러 1}: {대응 방안}
- {예상 에러 2}: {대응 방안}

---

## 검증 계약 (Verification Contract)

> Generator(구현자)와 Evaluator(검증자)가 사전에 합의하는 성공 조건.
> `/gap` 실행 시 이 목록을 기준으로 평가한다.

### 기능 검증 조건

| # | 조건 | 우선순위 |
|---|------|---------|
| 1 | {사용자가 X하면 Y가 되어야 한다} | Critical |
| 2 | {조건 설명} | Critical |
| 3 | {조건 설명} | Nice-to-have |

### 관통 검증 조건 (End-to-End)

> "저장됨" ≠ "사용 가능함". 데이터가 입력부터 최종 표시까지 관통하는지 검증한다.

| # | 시작점 (사용자 행동) | 종착점 (결과 확인) | 우선순위 |
|---|---------------------|-------------------|---------|
| 1 | {페이지A에서 데이터 입력/저장} | {페이지B에서 해당 데이터가 로드/표시됨} | Critical |
| 2 | {기능X 실행} | {기능Y에서 결과가 반영됨} | Critical |

<!-- 예시 — 프로젝트에 맞게 교체하세요
| 1 | 마이페이지에서 조건 저장 | /match 진입 시 조건이 자동 로드됨 | Critical |
| 2 | 시뮬레이션 실행 | /mypage/history에 의미 있는 결과 표시 | Critical |
| 3 | 관심 단지 등록 | /complex 지도에서 해당 단지 하이라이트 | Critical |
-->

### 역방향 검증 체크리스트
- [ ] 모든 Plan 요구사항이 설계에 반영되었는가?
- [ ] 설계의 각 컴포넌트가 Plan의 문제를 해결하는가?
- [ ] 누락된 엣지 케이스가 없는가?

### 평가 기준
- **기능**: 검증 조건이 모두 동작하는가?
- **설계 품질**: 구조가 일관되고 확장 가능한가?
- **단순성**: 불필요한 복잡도 없이 문제를 해결하는가?
